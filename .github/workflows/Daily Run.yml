name: Daily Run

on:
  schedule:
    - cron: '0 14 * * *'

env:
  DEVOPS_DIR: Google-Trending

jobs:
  GET:
    runs-on: windows-latest
    
    steps:
    - name: checkout
      uses: actions/checkout@v2
      with:
        repository: Azeem-Cole/Google-Trending
        token: ${{ secrets.GITHUB_TOKEN }}
        path: ${{ env.DEVOPS_DIR }}
    
    - name: Set up Python 3.7
      uses: actions/setup-python@v2
      with:
        python-version: 3.7
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas
        pip install pytrends
        
    - name: call file
      run: |
        python ${{ env.DEVOPS_DIR }}/google/googleSCRAPE/googleGET.py 
        python ${{ env.DEVOPS_DIR }}/google/googleSCRAPE/googlePARSE.py
        
        Set-location ${{ env.DEVOPS_DIR }}/google/googleSCRAPE
        Get-ChildItem
        
        Set-location ..
        Get-ChildItem 
        
        Set-location ..
        Get-ChildItem
        
        git config --global user.email "azeemcole70@gmail.com"
        git add .
        $date = Get-Date
        $datestr = "yyyy"+$date.Year+" "+"mm"+$date.Month+" "+"dd"+$date.Day
        git commit -m $datestr
        git push -u origin daily-grab
      # git init
      # git remote and origin https://github.com/Azeem-Cole/Google-Trending.git
      # git push -u origin Azeem-Cole-patch-1
        
      # $pwd
      # echo ${{ env.DEVOPS_DIR }}
      # gc ${{ env.DEVOPS_DIR }}/google/"Yr2020 mth11 day24"
      # Get-ChildItem
      # type "Yr2021 mth11 day22.txt"
        
        
   #   python ${{ env.DEVOPS_DIR }}/google/googleSCRAPE/googlePARSE.py
        
 
